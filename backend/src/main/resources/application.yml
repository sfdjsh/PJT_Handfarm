spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://ec2-3-39-143-94.ap-northeast-2.compute.amazonaws.com/handfarm?serverTimezone=UTC&useUniCode=yes&characterEncoding=UTF-8&amp
    username: ssafy
    password: ssafy
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      springframework:
        web: debug
    sql: debug
